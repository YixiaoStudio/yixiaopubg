{
  "entities": {
    "LeaderboardEntry": {
      "title": "Leaderboard Entry",
      "description": "A single entry in the game leaderboard.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the player."
        },
        "score": {
          "type": "integer",
          "description": "The rank or score achieved by the player."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the score was recorded."
        }
      },
      "required": ["name", "score", "timestamp"]
    }
  },
  "firestore": {
    "leaderboard": {
      "schema": "LeaderboardEntry",
      "description": "Collection of player scores and ranks."
    }
  }
}
